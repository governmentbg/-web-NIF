services:
    balance:
        image: haproxy:latest
        ports:
            - "8080:80"
        volumes:
            - ./:/usr/local/etc/haproxy:ro
        depends_on:
            - web
    web:
        ports: !reset []
